using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;
using System;

public class MDefine
{
    //private static Dictionary<string, System.Object> m_db = new Dictionary<string, System.Object>();

    //public static Dictionary<string, System.Object> db => m_db;

    //public static void Initdb()
    //{
    //    string sourceDirectory = Application.streamingAssetsPath + "/Config";

    //    try
    //    {
    //        var jsonFiles = Directory.EnumerateFiles(sourceDirectory, "*.json");

    //        foreach (string currentFile in jsonFiles)
    //        {
    //            string fileName = currentFile.Substring(sourceDirectory.Length + 1);
    //            System.Object cfgArry = JsonManager.LoadConfig<System.Object>(fileName);
                
    //            m_db.Add(fileName.Split('.')[1], cfgArry);
    //        }
    //    }
    //    catch (Exception e)
    //    {
    //        Console.WriteLine(e.Message);
    //    }
        
    //}
}
